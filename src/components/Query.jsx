import React from "react";

const Query = () => {
  return (
    <div>
      <p>Enter the URL to be shrinked : </p>
      <input type="text" />
      <button>Submit</button>
    </div>
  );
};

export default Query;
